<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.intelrobot">
    <!-- 允许发送请求 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BIND_SERVICE" />
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <service
                android:name="music.MusicService"
                android:enabled="true"
                android:exported="true">
        </service>

        <activity android:name="test.loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="test.RegisterActivity"></activity>
        <activity android:name="test.MainActivity"></activity>
        <activity android:name="test.PersonalInfoActivity"></activity>
        <activity android:name="music.MainActivity"></activity>
        <activity android:name="notepad.bean.NotepadActivity"></activity>
        <activity android:name="notepad.bean.RecordActivity"></activity>
        <activity android:name="Robot.ChatRobotActivity"></activity>
        <activity android:name="alter.AlterActivity"></activity>
       <activity android:name="alter.RecordActivity"></activity>
        <activity android:name="news.MainActivity"></activity>
        <activity android:name="news.NewsInfoActivity"></activity>
        <activity android:name="weather.MainActivity"></activity>
        <provider android:name="alter.NotepadProvider" android:authorities="com.example.intelrobot" android:enabled="true" android:exported="true"/>
        <receiver android:name="alter.MyReceiver" android:enabled="true" android:exported="true"/>
    </application>

</manifest>